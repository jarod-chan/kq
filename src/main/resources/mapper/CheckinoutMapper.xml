<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.fyg.zktime.domain.CheckinoutMapper">
	
	<select id="monthCheckinout" resultType="Checkinout">
		select id,checktime from checkinout 
		where userid=#{userid} and year(checktime)=#{year} and month(checktime)=#{month} order by checktime asc;
	</select>

</mapper>